name: anongram-server
type: custom

build:
  tool: dockerfile

deploy:
  command: npm start

resources:
  memory: 256
  disk: 1024

expose:
  - port: 80
    protocol: http

healthcheck:
  path: /
  port: 80
